version: '3.8'
services:
  db:
    image: postgres:14.1-alpine
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: exerciseDatabase
    healthcheck:
      test: "exit 0"

  fastapi:
    build: .
    ports:
      - "7654:8000"
    restart: always
    depends_on:
      db:
        condition: service_healthy

  adminer:
    image: adminer
    restart: always
    ports:
      - "7543:8080"

volumes:
  db:
    driver: local
